version: '3.9'

services:
  mongodb:
    container_name: mongo-db
    image: mongo
    ports:
      - 27017:27017
    environment:
      - MONGO_INITDB_ROOT_USERNAME=drhmanager
      - MONGO_INITDB_ROOT_PASSWORD=drhpass